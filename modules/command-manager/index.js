var commandManager = module.exports = {};

/**
 * Routes commands with callbacks
 * @param command The callback was triggered when a user send this string.
 * @param cb The callback function
 * @returns {*}
 */
commandManager.on = function (command, cb) {
    return null
};