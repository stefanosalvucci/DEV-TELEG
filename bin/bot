#!/usr/bin/env node

require('es6-promise').polyfill(); // EcmaScript6 Promise compatibility for old versions of Node.js
var database = require('../modules/database');

database.connect().then(function (db) {
    require('../modules/bot-app-controller');
}).catch(function (err) {
    throw err
});